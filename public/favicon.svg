<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
    <!-- The masthead's brand mark (`.brand-mark` in src/styles/global.css): two
         accent rules with nothing between them, because the product was never
         built. Thickened well past the header's hairlines — 1px scales to
         nothing at the 16px size that actually matters. -->
    <rect width="128" height="128" rx="24" fill="#121014" />
    <rect x="36" y="20" width="14" height="88" fill="#ff570a" />
    <rect x="78" y="20" width="14" height="88" fill="#ff570a" />
</svg>
