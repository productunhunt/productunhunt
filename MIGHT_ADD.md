# Might add

Ideas for the site that are parked, not rejected. Revisit each once the real
ideas are in and we can see whether it earns its place.

## Relapse links — SHIPPED

Built as sketched: optional `relapse: { url, extent }` frontmatter, the
`author-relapsed` status ("Built it anyway" badge), the "Evidence of relapse"
dossier block with `nofollow`, the swapped founder note on relapsed pages, and
the CI rule that a relapse block and `unbuilt` never coexist.

First offender: cuegence (`extent: landing-page`). When macrolog's writeup
lands, it takes `extent: working-app` — "It has users, which is worse."
