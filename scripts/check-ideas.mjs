// Content lint for `src/content/ideas/`. Fleshed out in Phase 2, once the
// collection and the required section headings exist; wired into CI now so the
// step is in place from the first green build.
process.exit(0);
